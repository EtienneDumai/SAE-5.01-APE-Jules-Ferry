/**
 * Fichier : frontend/src/app/models/Auth/login-credentials.ts
 * Auteur : cf ~/docs/general/participants.md
 * Description : Ce fichier decrit le modele login credentials utilise dans le frontend.
 */

export interface LoginCredentials {
  email: string;
  mot_de_passe: string;
}